package com.nds.droidtv2

/**
 * Repository used to access all database and network data. The repository coordinates data updates
 * from the web service and updates into the local database. Requested data is retrieved from the
 * database as the single source of truth for all data.
 */
class Repository {
    //TODO Implement
}